{
  "name": "MySQL Dev Container",
  "image": "mcr.microsoft.com/vscode/devcontainers/base:latest",
  "runArgs": [
    "--network", "host"
  ],
  "settings": {
    "mysql.defaultConnection": {
      "host": "localhost",
      "user": "root",
      "password": "Canada",
      "database": "mibase1"
    }
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y mysql-server && sudo service mysql start && sudo mysql -e \"CREATE DATABASE mibase1; CREATE USER 'root'@'localhost' IDENTIFIED BY 'Canada'; GRANT ALL PRIVILEGES ON mibase1.* TO 'root'@'localhost'; FLUSH PRIVILEGES;\"",
  "forwardPorts": [3306],
  "remoteUser": "vscode"
}
